Your task is to generate a chained sequence of tool calls as a tool planning agent. Write your output between the <tool_calls> and </tool_calls> tags.

## Context Provided:

<tools> - Available tools you can use
<persona> - The personality profile to match when creating tool calls
<date> - Current date for time-sensitive operations
<length> - Maximum number of tool calls to generate
<prior_tool_calls> - Previously executed tool calls (if any)

## Output Format:

<tool_calls>
[
    {
        "name": "tool_name",
        "arguments": {
            "param1": "value1",
            "param2": "$1.output_field"
        },
        "id": "$1"
    },
    {
        "name": "another_tool",
        "arguments": {
            "param": {% if allow_nested %}"$1.result"{% else %}"$2.output_field"{% endif %}
        },
        "id": "$2"
    }
]
</tool_calls>

## Requirements:

1. **Match the persona**: Generate tool calls as if made by the individual in <persona>

2. **ID numbering**:
   - Use sequential IDs: "$1", "$2", "$3", etc.
   - If <prior_tool_calls> exists, continue numbering from the last ID in that list

3. **Make tool calls complex**: Include several required arguments per tool call, not just the minimum

4. **Use realistic values**: Provide concrete, plausible argument values - no placeholders or hypothetical data

5. **Preserve date/time formatting**: Copy dates and times exactly as provided - don't reformat

6. **Generate appropriate length**: Create up to <length> tool calls, but fewer is acceptable if more logical
{% if allow_nested %}
7. **CRITICAL - Chain tool calls**: Each tool call after the first MUST reference output from earlier calls
   - Reference outputs using dot notation: `"$1.output_parameter"`
   - Can reference any prior call from current <tool_calls> OR from <prior_tool_calls>
   - If <prior_tool_calls> exists, at least one new tool call MUST reference an output from it
   - Only reference `output_parameter` fields, never input `parameter` fields
{% endif %}
{% if allow_nested %}8{% else %}7{% endif %}. **Avoid repetition**: Don't reuse tools already called in <prior_tool_calls>